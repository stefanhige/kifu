cmake_minimum_required(VERSION 3.13)
project(eigensrc-download NONE)

include(ExternalProject)
ExternalProject_Add(eigen
    GIT_REPOSITORY https://github.com/libigl/eigen.git
    GIT_TAG master
    SOURCE_DIR "${CMAKE_CURRENT_SOURCE_DIR}/${LIB_DIR}/${EIGEN_DOWNLOAD_DIR}"
    BINARY_DIR "${CMAKE_CURRENT_SOURCE_DIR}/${LIB_DIR}/${EIGEN_BUILD_DIR}"
    #CONFIGURE_COMMAND ""
    #BUILD_COMMAND ""
    #INSTALL_COMMAND ""
)

